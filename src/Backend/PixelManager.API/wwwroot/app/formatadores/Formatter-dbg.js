sap.ui.define([
], () => {
    "use strict";

    return {
        formatarTipoArquivo: function () {
            debugger
            // for (var nome in TipoDeArquivo) {
            //     if (TipoDeArquivo[nome] === tipo) {
            //         return nome;
            //     }
            // }
            return "testeTipo";
        }
    };
});
